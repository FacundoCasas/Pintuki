//URLS para consumir del Back
//Llenar con la IP, poner ip config en la  consola, levantar con expo start -tunnel
const HOST_URL = 'http://ip:3000';

const API_PUBLICACIONES = HOST_URL + '/publicaciones';

export { API_PUBLICACIONES }